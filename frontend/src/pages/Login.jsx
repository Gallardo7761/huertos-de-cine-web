import LoginForm from "@/components/Auth/LoginForm";

const Login = () => {
    return (
        <main className="container my-5">
            <LoginForm />
        </main>
    );
}

export default Login;